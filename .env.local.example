# Get a free public token at https://account.mapbox.com/
# Required only if you want the interactive map on the /partners page.
# Without it, the page still works fully (search, tabs, cards) but the map
# panel shows a friendly placeholder instead.
NEXT_PUBLIC_MAPBOX_TOKEN=
